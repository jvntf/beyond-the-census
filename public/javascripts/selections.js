// methods to modify the selection state - used to hide/show/recolor objects on the map, list, and info card

addSelectionSingle(id) {
  state.languageSelection.push(id)
}

removeSelectionSingle() {
  
}
